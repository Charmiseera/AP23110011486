# Affordable Evaluation Backend

## Setup
1. Clone the repository.
2. Run `npm install` to install dependencies.
3. Create a `.env` file and add your `TOKEN`.

## How to Run
Run the server with:
`npm start`
